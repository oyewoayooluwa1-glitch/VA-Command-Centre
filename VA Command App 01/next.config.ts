import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Runs on Cloudflare Workers via the OpenNext adapter (see wrangler.jsonc).
  // Keep this file minimal until a specific need (images, redirects) comes up.
};

export default nextConfig;
